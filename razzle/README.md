# Razzle Example Project

## Steps To Reproduce Bug

1. Run project with `yarn start` and open localhost:3000.

2. Click `AnchorButtonPage` link.

3. AnchorButton will have zero padding even though the size is specified.
